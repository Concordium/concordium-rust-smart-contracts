[template]
cargo_generate_version = "0.16.0"

[placeholders]
description = { type="string", prompt="Description for the crate?" }
currentAuthors = { type="string", prompt="Authors for this crate?" }
TOKEN_METADATA_BASE_URL = { type="string", prompt="Token metadata base URL of the token?", default='https://some.example/token/' }
